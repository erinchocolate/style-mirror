# Samples Index

The user's authentic writing, collected at onboarding, used to **seed** the style profile as weak priors (`confidence = seed`, `evidence = 0`). Samples are never edited by the agent — they're raw ground truth.

| date | file | scenarios covered | notes |
|------|------|-------------------|-------|
| _(none yet — created by ONBOARD)_ | | | |

---

### Example row (delete once real rows exist)

```
| 2026-06-03 | 20260603_seed_samples.md | email_to_boss, team_chat, doc | 5 samples |
```

Add more samples anytime by appending a new dated file and a row here, then re-running a light seed pass. Real polish→final diffs (via LEARN) always outweigh samples — samples only get you off the ground.
